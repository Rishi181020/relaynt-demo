name: DevOps Demo Pipeline

on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install PyJWT
      - name: Run Tests
        run: |
          # python test_app.py
          # python test_calibration_drift.py
          # python test_app_scrape.py
          python test_cve_audit.py
